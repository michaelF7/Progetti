from os import system # Dalla libreria os importo system.

system("cls") # Comando per pulire la consore. 

a = float(input("Inserisci un numero: ")) # Input di a. 
b = float(input("Inserisci un numero: ")) # Input di b. 

# Controllo se a è uguale a zero
if a == 0: print("L’equazione è interminata o impossibile”") # Output se a è uguale a zero.
    
else:
    x = - b / a # Operazione matematica.
    print(x) # Output di x.